<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Recruiter Home</title>
    <link
            href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta2/dist/css/bootstrap.min.css"
            rel="stylesheet"/>
</head>
<body>
<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
        <a class="navbar-brand" href="/recruiters/<%=idRecruiter%>">Recruiter Panel</a>
        <button
                class="navbar-toggler"
                type="button"
                data-bs-toggle="collapse"
                data-bs-target="#navbarNav"
                aria-controls="navbarNav"
                aria-expanded="false"
                aria-label="Toggle navigation"
        >
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" href="/recruiters/<%=idRecruiter%>/job-offers/history">Manage Offers</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/recruiters/<%=idRecruiter%>/organisation-registration-request"
                    >Add Organisation</a
                    >
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/recruiters/<%=idRecruiter%>/join-organisation"
                    >Join Organisation</a
                    >
                </li>
                <li class="nav-item">
                    <a href="/recruiters/<%=idRecruiter%>/create-job-description" class="nav-link">Create Job Description</a>
                </li>
                <li class="nav-item">
                    <a href="/recruiters/<%=idRecruiter%>/create-job-offer" class="nav-link">Create Job Offer</a>
                </li>

            </ul>
            <a class="btn btn-danger d-flex ms-auto" href="/logout">
                Log Out
            </a>
        </div>
    </div>
</nav>

<%if (jobOffers.length > 0) {%>
<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="mb-3">
            <div class="list-group">
                <% jobOffers.forEach((jobOffer)=>{%>
                    <div class="list-group-item list-group-item-action flex-column align-items-start my-2">
                        <div class="d-flex w-100 justify-content-between">
                            <h5 class="mb-1"><%= jobOffer.title%></h5>
                            <small><%= jobOffer.jobOfferStatus%></small>

                        </div>
                        <p class="mb-1"><strong><%= jobOffer.organisationName%></strong></p>
                        <p class="mb-1"><%= jobOffer.description %></p>
                        <p class="mb-1"><%= jobOffer.deadline %></p>
                        <% if (jobOffer.status !== 'expired') {%>
                        <a href="/recruiters/<%=idRecruiter%>/manage-job-applications/<%=jobOffer.id%>" class="btn btn-primary"
                        >Manage Applications</a
                        >
                        <% }%>
                    </div>
                <% });%>
            </div>
        </div>
    </div>
</div>
<%} else {%>
    <div class="d-flex justify-content-center align-items-center vh-100">
        <div class="alert alert-danger" role="alert">
          No Offer to manage !
        </div>
      </div>
      <% } %>
</body>
</html>
